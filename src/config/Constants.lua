-- Centralized name constants
local Constants = {
    SCENE = {
        MENU = "scene:menu",
        GAME = "scene:game"
    },
    GAME_MODE = {
        CLASSIC = "gameMode:classic",
        ROGUE = "gameMode:rogue"
    }
}
return Constants
